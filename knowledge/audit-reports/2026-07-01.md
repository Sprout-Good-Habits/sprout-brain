# sprout-brain audit — 2026-07-01

brain_sha: fa639ae (pre-audit)
sprout_app_sha: 61871428f (origin/main)
mcp_surface: available (sprout MCP connected)

## Summary
- Mirror:       in-sync — `canvas/sdk.md` (only the sanctioned `host.ts` drop differs), `canvas/artifact-kit.md` (byte-identical)
- Capabilities: 0 tools no longer documented; 1 authoring tool uncovered (`canvas_prepare_upload`) added; 3 domains uncovered (screentime / program / marketplace-tools) — surfaced, not auto-created
- Availability: 2 stale prohibitions softened (external-scripts, external-fetch); 3 Released surfaces added to the canvas capability doc (rive, openExternalUrl, signal)
- Staleness:    0 docs past 60d threshold (2 docs re-verified + bumped this run as a side effect of fixes)

## Round 2 — thorough per-capability review (2026-07-01)

Verified every capability doc one-by-one against the live MCP tool defs on sprout-app origin/main (one grounding agent per doc). Dominant drift was **missing tools** — the early docs never kept up with the growing MCP surface. All fixed; `Last verified` bumped to 2026-07-01.

- `family-and-child-lookup.md` — DRIFTED → added `family_list`, `family_select_context` (only `family_query_overview` was listed); documented the multi-family list→select flow + the overview's children/preferences shape.
- `skill.md` — DRIFTED → added `skill_update`/`skill_get`/`skill_list`/`skill_post_result` (only write+invoke listed); added the `handsReferenced` Run-vs-Assignment rule; noted home-agent skills aren't heartbeat-schedulable / invoke-runnable.
- `task-and-review.md` — DRIFTED → added `task_list`/`task_update`/`task_delete`; corrected the gem claim (`gemsAwarded` is optional — approval never auto-credits gems).
- `conversation-task.md` — DRIFTED → fixed goalType enum names (`share`/`explain`/`debate`/`practice`, not "journaling"); documented required `scheduleSpec`/`childIds`, optional `assignmentSkillId`, and the `conversationSpec` tuning fields (min/maxResponses, dailyTarget, effort, grading, outcomes).
- `heartbeat.md` — DRIFTED → added `heartbeat_list`; dropped stale "when available" hedges (create/update are unconditionally exposed).
- `reward-and-gems.md` — DRIFTED → added `reward_prepare_photo_upload`, `gems_list_transactions`; `gems.adjust` → `gems_adjust` (unconditional, hedge removed). (Screen-time reward category added in the prior commit.)
- `home-agent-boundary.md` — **CLEAN**, no change.
- `canvas.md`, `program.md`, `screentime.md`, `marketplace.md` — already freshly grounded earlier this session.

## Applied fixes
- `knowledge/capabilities/canvas.md` — corrected two over-absolute constraints: "do not add external scripts" now names the canvas-CDN proxy as the sanctioned path; "cannot fetch external data" now carves out same-origin bundle-asset reads. Added Released surfaces the doc omitted: `sprout.rive.resolveAsset` (curated device+preview / bundle device-only / no text-gen), `sprout.openExternalUrl` (Khan/Duo allowlist, device-only), `sprout.signal`. Added `canvas_prepare_upload` to the tool list. Bumped `Last verified` → 2026-07-01 (re-read against the SDK source of truth this run).
- `knowledge/anti-patterns/canvas-fetches-external-data.md` — added a "Not this anti-pattern" section distinguishing allowed same-origin bundle reads and the `openExternalUrl` server-authorized *launch* (not a progress fetch) from the real anti-pattern. Bumped `Last verified` → 2026-07-01.

## Surfaced for human decision → RESOLVED
- **Domain coverage scope.** The `sprout` MCP surface exposes `screentime_*`, `program_*`, and `marketplace_*` (submit/fork/adopt) tools to a parent's agent, but `knowledge/capabilities/` had no dedicated docs for screen-time control, programs, or the marketplace *tool* surface (marketplace was covered only as a solution *pattern* in `patterns/adopt-or-remix.md`).
- **Decision (Han, 2026-07-01): create the new capability domains.** Added `capabilities/program.md`, `capabilities/screentime.md`, `capabilities/marketplace.md`, each grounded in the real MCP tool definitions at sprout-app origin/main (tool names + guardrails; no invented fields). Indexed them in `current-platform.md` (capability list + three new recommendation-matrix rows) and `llms.md`. Marketplace `search`/`inspect` documented as feature-gated (off by default).

## Raw findings

### Mirror
- `canvas/sdk.md`: upstream 1249 lines, brain 1249 lines. `diff` shows a single hunk — line 1205, the sanctioned internal `host.ts` / `@sprout/canvas/web` pointer drop. In sync.
- `canvas/artifact-kit.md`: upstream 380 lines, brain 380 lines, `diff` empty. In sync.

### Capabilities (vs live sprout MCP surface)
- Canvas tools documented: create/update/list/get. Live surface also has `canvas_prepare_upload` (authoring-relevant, added), plus delete/get_scan_status/runs_list/runs_get (operational, not architect-facing — left out by design).
- No documented tool is missing from the live surface (no dead tool references).
- Uncovered domains: screentime, program, marketplace-tools (see "Surfaced for human decision").

### Availability (SDK source of truth: packages/sprout-canvas/src on origin/main)
- Wired + host-backed (Released): `whoami`, `openExternalUrl`, `tts.speak/stop`, `rive.resolveAsset` (curated), `signal`, `complete`/`score`/`timed`, `state`. Verified `rive.resolveAsset` is defined in `runtime-core.ts` with a server route (`canvas-rive-assets.route.ts`), web-preview resolver, and iOS host wiring — not just a type.
- Typed-but-rejects (Roadmap): `getAsset`, `uploadAsset`, `history`, `recall`. Correctly labelled Roadmap in `canvas/sdk.md` (fixed in the mirror sync commit `fa639ae`).
- Stale prohibitions found + softened: canvas.md "do not add external scripts" (canvas-CDN proxy is the carve-out) and "cannot fetch external data" (same-origin bundle reads allowed). No stale WASM/Rive prohibition exists in any anti-pattern doc.

### Staleness (threshold 60d; today 2026-07-01)
- `current-platform.md` 2026-06-02 (29d) — fresh. `unavailable-patterns.md` 2026-06-02 (29d) — fresh. Nothing past threshold. The two docs edited this run had their dates bumped because their content was re-verified against ground truth, not as blind bumps.
